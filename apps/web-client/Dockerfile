# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build web-client`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t web-client`.
FROM docker.io/nginx:mainline-alpine3.20-slim

WORKDIR /usr/share/nginx/html

# RUN addgroup --system web-client && \
#     adduser --system -G web-client web-client

COPY dist/apps/web-client/browser .
